version: '3.8'

services:
  ml_app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./artifacts:/app/artifacts
    environment:
      - PYTHONPATH=/app